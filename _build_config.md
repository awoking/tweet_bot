# Cloudflare Pages Build Configuration

# Build command
pip install -r requirements.txt && python tweet_bot.py

# Output directory (空でも可)

# Environment variables
# TWITTER_API_KEY = (set in Cloudflare dashboard)
# TWITTER_API_SECRET = (set in Cloudflare dashboard)
# TWITTER_ACCESS_TOKEN = (set in Cloudflare dashboard)  
# TWITTER_ACCESS_TOKEN_SECRET = (set in Cloudflare dashboard)
# TWITTER_BEARER_TOKEN = (set in Cloudflare dashboard)